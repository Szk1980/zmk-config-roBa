/ {
    /* 左にはポインタを持たせない */
    chosen {
        zmk,pointing-device = &none;
    };
};

/* 右用ポインタで使っているバスが L に来ていたら無効化 */
&spi1 { status = "disabled"; };
&i2c1 { status = "disabled"; };

/* すでに encoder ノードがあるなら残す。無ければ別途 pins 設定が必要（CKW12のA/B/押下のGPIO） */
