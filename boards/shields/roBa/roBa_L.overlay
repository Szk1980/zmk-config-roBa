/* 左エンコーダのA/Bピンを明示して上書きする */
&left_encoder {
    status = "okay";
    /* ここにあなたの環境の a/b を入れる。まずは「a↔b を反転」して試す */
    a-gpios = <&xiao_d B_PIN GPIO_ACTIVE_HIGH>;  /* ← 今の b の値 */
    b-gpios = <&xiao_d A_PIN GPIO_ACTIVE_HIGH>;  /* ← 今の a の値 */
}
