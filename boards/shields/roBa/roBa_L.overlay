default_layer {
    bindings = < …省略… >;
    sensor-bindings = <&inc_dec_kp &kp PAGE_UP &kp PAGE_DOWN>;
};

ARROW {
    bindings = < …省略… >;
    sensor-bindings = <&inc_dec_kp &kp LC(PAGE_UP) &kp LC(PAGE_DOWN)>;
};

MOUSE { bindings = < …省略… >; };
SCROLL { bindings = < …省略… >; };

/* エンコーダ（左ノブ） */
encoders {
    left_encoder_binding {
        device = <&encoder_left>;
        bindings = <&inc_dec_kp &kp PAGE_UP &kp PAGE_DOWN>;
        // press-binding = <&kp MUTE>;
    };
};
